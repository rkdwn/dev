import { Typography } from "@/components/common";

const Main = () => {
  // login check
  // 로그인 되어있으면 메인페이지 아니면 로그인페이지
  return (
    <>
      <Typography>MAIN</Typography>
    </>
  );
};

export default Main;
